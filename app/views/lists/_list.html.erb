<div class="card m-2">
  <%= cl_image_tag list.photo.key, alt: "text", :class => "card-img-top" if list.photo%>
  <div class="card-body">
    <h5 class="card-title"><%= list.name %></h5>
    <%= link_to 'Details', list_path(list), :class => "btn btn-primary" %>
  </div>
</div>
